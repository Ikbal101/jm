const numbers = [84,85,92,94];
const student = {
    name:"Ikbal Ahmed",
    class:"Ten",
    Roll:07,
    Year:2021
};

const makeResult = `Ikbal Ahmed got ${numbers} numbers in the exam.`;
console.log(makeResult);

const studentDetails = `${student.name} is the student name and his class and roll is: ${student.class} , ${student.Roll}.`;
console.log(studentDetails);

